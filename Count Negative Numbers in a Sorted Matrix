class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        pn=0
        for i in grid:
            for j in range(len(i)):
                if i[j]<0:
                    pn+=1
        return pn
